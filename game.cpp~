
#include <iostream>
#include <cstring>
#include <cctype>

using namespace std;

int strip(input){
  char temp[81];
  int length;
  length = strlen(input);
  for (int i < length, i++){
    if(isalnum(str[i])){
      strcat(temp,tolower(str[i]));
    }
  }
  strcpy(temp,input);
  return 0;
}

int reverse(){}

int main(){
  char input[81];
  input = cin.get(81);
  cin.ignore(9999999,"\n");
  strip(input);
  cout << input;
  return 0;
}
